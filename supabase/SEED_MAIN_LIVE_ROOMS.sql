-- Run in Supabase Dashboard → SQL Editor (or: npm run db:seed-rooms)
-- Deletes ALL live rooms, creates 10 permanent category orbits on the lobby map.
--
-- NOTE: Do NOT use \ir — that is psql-only. Paste RUN_SEED_MAIN_LIVE_ROOMS.sql instead.

-- See: supabase/RUN_SEED_MAIN_LIVE_ROOMS.sql (full self-contained script)
